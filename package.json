{
  "name": "ifexists",
  "version": "1.0.0",
  "description": "executes a callback if an specific element exists on page",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
